# QUICK INSTALLATION GUIDE
# DepEd Southern Leyte Division Library Log System

## STEP-BY-STEP INSTALLATION

### 1. PREPARE XAMPP
- Make sure XAMPP is installed.
- Start Apache and MySQL from XAMPP Control Panel.

### 2. CREATE DATABASE
1. Open browser: http://localhost/phpmyadmin
2. Click the "Import" tab.
3. Choose file: `database.sql`
4. Click "Go".
5. Database `library_logs` will be created with 6 tables:
- `users` (admin accounts)
- `districts` (district master list)
- `schools` (school master list linked to districts)
- `client_types` (verified visitor categories)
- `personnel` (verified division/field personnel)
- `logbook_entries` (visitor logs)

### 3. COPY FILES TO XAMPP
1. Go to: `C:\xampp\htdocs\` (or your XAMPP location)
2. Create folder: `library-system`
3. Copy all project files into this folder.

### 4. ACCESS THE SYSTEM
- Public Page: http://localhost/library-system
- Admin Login: http://localhost/library-system/admin/login.php

### 5. DEFAULT LOGIN
- Username: `admin`
- Password: `admin123`

### 6. OPTIONAL: REFRESH VERIFIED MASTER DATA
1. Replace source files in `docs\`:
   - `Database_2025-2026.xlsx`
   - `List_of_Divisionb_Personnel.docx`
2. Regenerate:
   - `docs\verified_district_school_master.csv`
   - `docs\verified_personnel_master.csv`
3. Rebuild/import `database.sql` into `library_logs`

## FOLDER STRUCTURE (IMPORTANT FILES)
```
C:\xampp\htdocs\library-system\
|-- index.php
|-- database.sql
|-- README.md
|-- INSTALLATION.txt
|-- admin\
|   |-- login.php
|   |-- dashboard.php
|   |-- logout.php
|   `-- export.php
|-- config\
|   |-- database.php
|   `-- session.php
|-- css\
|   |-- style.css
|   `-- admin.css
|-- includes\
|   |-- bootstrap.php
|   |-- helpers.php
|   `-- partials\
|       |-- document_start.php
|       |-- document_end.php
|       `-- flash.php
`-- js\
    `-- main.js
```

## HOW IT WORKS

VISITOR SIDE:
- Visitor opens: http://localhost/library-system
- Fills in: Name, Client Type, Position, Organization Category, then District + School (if applicable), Purpose
- Clicks "SUBMIT LOG"
- Date and time are automatically recorded
- Single visit entry only (no time-out tracking)
- Done. No login needed.

ADMIN SIDE:
- Admin opens: http://localhost/library-system/admin/login.php
- Logs in with: admin / admin123
- Views all visitor logs in dashboard
- Opens master data page: http://localhost/library-system/admin/master_data.php
- Manages client types and personnel records
- Can filter by date, name, client type, district, school, and organization text
- Can export logs to CSV
- Can delete entries if needed

## COMMON ISSUES

Problem: "Cannot connect to database"
Solution: Make sure MySQL is running in XAMPP

Problem: "Page not found"
Solution: Check if files are in correct folder (htdocs\library-system)

Problem: "Cannot login"
Solution: Make sure you imported database.sql first

Problem: "Table doesn't exist"
Solution: Re-import database.sql in phpMyAdmin

## NEED HELP?
Read the full README.md file for detailed instructions.
